
 {% extends 'SiteAdminBundle::layout.html.twig' %}


 {% block content %}
{{ parent() }}

<h1>Editer une notification</h1>

<form action="{{ path('notifications_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-primary"><i class="icon-pencil"></i>Editer cette notification</button>
    </p>
</form>

 {% endblock %}